# 亮点 / 分发 / SEO 落地清单

> 核心问题：通用翻译红海里，没亮点没人用、没人知道也白做。本文件给「卖点」和「让人知道」的可执行路径。

## 一、亮点（产品楔子）：AI 润色

已落地的 PRO 权益：**翻译完一键「润色」成母语级表达**，支持 4 种语气——
`地道 / 商务 / 学术 / 简洁`。接便宜 LLM（DeepSeek / 通义千问 / GLM，约 ¥1/百万 token），
密钥只在 `worker.js` 后端，前端永不接触（见 `worker.js` 的 `/ai-polish`）。

**为什么它是卖点：**
- Google / 有道 / DeepL 给的是「字面翻译」；「母语级润色」是肉眼可见的增量，截图即懂。
- 成本极低（LLM API 几乎免费量级），却能撑起付费理由，和「输入字数 10×」叠加成完整 PRO 价值。
- 演示门槛低：翻译一句话 → 点润色 → 对比原文，短视频 10 秒说清。

**可叠加的垂直品牌（选一个主打）：** 论文翻译、简历/求职翻译、跨境电商客服话术、追剧字幕、
旅游随拍。选定后 UI 预设 + 文案都围着它转，SEO 也更好做（搜「论文翻译」的人精准）。

## 二、别人怎么知道（分发）

1. **SEO（搜索引流）** —— 但注意：当前是 Flutter Web（SPA），GitHub Pages 上搜索引擎几乎抓不到内容。
   - 选 3–5 个长尾关键词做落地页：`论文翻译`、`日语简历翻译`、`英文邮件润色` 等。
   - 把站点迁到 **Cloudflare Pages / Vercel** 并做预渲染（Flutter 可用 `flutter build web` + 预渲染/SSR 方案，或单独用 Astro/纯 HTML 写个静态落地页引导到 App）。
   - 落地页放「润色前后对比」截图 + OG 标签（分享到微信/小红书有图有标题）。
2. **内容社区种草**：小红书 / 抖音 / B站 发「翻译完一键变母语级」短视频；少数派、V2EX、掘金 发帖。
3. **兑换码裂变**：已有 `redeem` 体系，给早期用户 / KOL 送 PRO 兑换码换口碑（见 `PAYMENT_GO_LIVE.md`）。
4. **免费基础 + PRO 转化**：免费够用但有限（500 字），润色等亮点锁 PRO。

## 三、上线前别漏

- **隐私政策页**：微信/支付宝商户入驻、App 上架都要求，补一个页面并在付费墙/关于里挂链接。
- **AI 润色防盗刷**：`worker.js` 的 `aiPolish` 目前是演示级开放（权益仅存前端）。上线前务必加：
  会员 token 校验 + 限频（如每用户每分钟 N 次），否则可被绕过白嫖 LLM。
- **SEO 真实化**：SPA 不预渲染 = 没 SEO，先解决「迁移 + 预渲染」再投内容。

## 四、下一步建议

- 先定一个垂直场景（决定品牌名/落地页文案）。
- 把站点迁 Cloudflare Pages 并加预渲染落地页。
- 拍 2–3 条「润色对比」短视频发出去。
